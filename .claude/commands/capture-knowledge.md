---
description: Capture tribal knowledge from this session into CLAUDE.md or a docs/ file.
---

Re-read this session and ask: was anything learned about this project that is non-obvious from reading the code — a constraint, a gotcha, a decision and its reason, a wrangler/D1/Cloudflare quirk?

1. If there are **no** candidates that fit that bar, say so plainly and stop.
2. Otherwise list candidates as 1-line bullets. For each, propose whether it belongs in `CLAUDE.md` (project-wide) or a new `docs/<topic>.md` file (if it's large enough to warrant its own page).
3. Ask which to capture (all / some / none) and confirm paths.
4. For each accepted fact, append to the chosen file. Show the diff. Do not stage or commit — let the developer decide.
